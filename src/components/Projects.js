import React from 'react'
import "./../css/Sections.css"

const Projects = (props) => {

  return (
    <section id='project' ref={props.ref} className='section project'>Projects</section>
  )
}

export default Projects